import { Box, Container } from "@mui/material";

const SideBar = () => {
  return (
    // <Container maxWidth="xs">
    <Box bgcolor="lightgreen" sx={{ width: "15vw" }}>
      SideBar
    </Box>
    // </Container>
  );
};

export default SideBar;
